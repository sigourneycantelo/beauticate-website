# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/

# env files — never commit real secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# data synced from Shopify — regenerated by script
data/shopify-products.json
data/shopify-collections.json
data/shopify-brands.json

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# pagefind index (generated at build)
public/pagefind/


# WP audit cache
.cache/

# WordPress XML export — too large for git (214MB), local only
migration/
.firecrawl/

# Regenerated by scripts/audit-testimonials.mjs / audit-extract-lines.mjs.
# Tracking them caused a cherry-pick conflict on every push to main and they
# carry nothing the scripts cannot rebuild. tga-review.xlsx stays tracked:
# it is the human deliverable and records which fixes were approved.
docs/audit/testimonial-audit.csv
docs/audit/testimonial-audit.md
docs/audit/lines-to-fix.json
